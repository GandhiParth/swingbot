nse_ins.remove(pl.col("segment") == "INDICES")
    .with_columns(
        pl.col("symbol")
        .str.split(by="-")
        .list.get(index=1, null_on_oob=True)
        .fill_null("EQ")
        .alias("suffix")
    )
    .filter(pl.col("suffix").is_in(["EQ"]))
    .select("symbol", "instrument_token")
    .sort("symbol")